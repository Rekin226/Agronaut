# Virtual environments
.venv/
venv/
test_venv/
env/

# Python build / cache
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
.pytest_cache/

# Tooling / local agent state (machine-specific, do not commit)
.gstack/
.claude/settings.local.json

# Generated databases / caches
chroma_db/
srcs/chroma_db/
faiss_document_store.db
web_cache.sqlite
data/*.sqlite3
data/*.sqlite3-*   # WAL/SHM sidecar files

# Local models
bge-large-en/

# Raw datasets (large; fetch with scripts/fetch_aquaponics_data.py).
# The derived data/empirical_envelope.json IS committed.
data/raw/

# Secrets / local environment (NEVER commit API tokens)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
